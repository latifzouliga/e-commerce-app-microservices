spring:
  data:
    mongodb:
      username: zouliga
      password: zouliga
      host: localhost
      port: 27017
      database: customer
      authentication-database: admin
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup,orderGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.common.serialization.StringSerializer
      value-deserializer: org.springframework.kafka.support.JsonSerializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.type.mapping: orderConfirmation:com.zouliga.kafka.OrderConfirmation,paymentConfirmation:com.zouliga.notification.PaymentNotificationRequest
  mail:
    host: localhost
    port: 1025
    username: zouliga
    password: admin
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
server:
  port: 8040