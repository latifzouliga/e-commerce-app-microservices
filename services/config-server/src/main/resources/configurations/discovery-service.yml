eureka:
  instance:
    hostname: localhost
  eureka:
    register-with-eureka: false
    client:
      service-url:
        defaultZone: "http://localhost:8761/eureka/"
    instance:
      prefer-ip-address: true

server:
  port: 8761